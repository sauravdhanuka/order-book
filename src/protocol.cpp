#include "protocol.h"

// All protocol functions are inline in the header.
// This compilation unit exists for potential future extensions
// and to satisfy the CMake target requirements.

namespace ob {
// Placeholder for any non-inline protocol helpers in the future.
} // namespace ob
